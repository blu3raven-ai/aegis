"""Inbound webhook utilities — signature primitives, a generic handler,
SCM receiver routes (providers/), the body normalizer, and the dispatch
listener that turns receiver events into CI scan submissions.
"""
