"use client"

import { ActivityView } from "@/components/shared/activity/ActivityView"

// History tab — the full activity feed. The shared "Inbox" header and the
// Triage/History tab bar live in the inbox layout.
export default function InboxHistoryPage() {
  return <ActivityView />
}
