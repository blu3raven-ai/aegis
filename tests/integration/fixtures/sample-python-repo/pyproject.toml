[project]
name = "sample-python-repo"
version = "0.0.1"
description = "Minimal Python project used by the scanner-http integration workflow."
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]
